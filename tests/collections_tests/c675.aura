// Collections: Pipes & Chains
let data = [6, 10, 23, 19, 2, 13, 5, 21, 12];

let result = data
    |> filter((x) => x % 2 == 0)
    |> map((x) => x * 2)
    |> filter((x) => x > 10);

print(result);
