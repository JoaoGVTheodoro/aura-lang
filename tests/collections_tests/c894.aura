// Collections: Grouping
let numbers = [1, 2, 2, 3, 3, 3, 4, 4];
let grouped = {};

for num in numbers {
    let key = "num_" + num;
    if key in grouped {
        grouped[key] = [*grouped[key], num];
    } else {
        grouped[key] = [num];
    }
}

print(grouped);
