// Collections: Transformations
let numbers = [4, 19, 13, 1, 5, 12, 6, 18];

let sum = numbers |> reduce(0, (acc, x) => acc + x);
let product = numbers |> reduce(1, (acc, x) => acc * x);

print(sum);
print(product);
