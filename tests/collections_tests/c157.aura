// Collections: Pipes & Chains
let data = [4, 10, 24, 26, 9, 17, 27, 9, 18, 8];

let result = data
    |> filter((x) => x % 2 == 0)
    |> map((x) => x * 2)
    |> filter((x) => x > 10);

print(result);
