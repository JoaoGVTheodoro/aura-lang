// Collections: Pipes & Chains
let data = [27, 23, 21, 15, 9, 23, 4, 16, 12, 24];

let result = data
    |> filter((x) => x % 2 == 0)
    |> map((x) => x * 2)
    |> filter((x) => x > 10);

print(result);
