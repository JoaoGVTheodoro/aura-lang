// Speed Test 108
let limit = 1000; // This will be multiplied by stress_level in test
let mut sum = 0;
for k in 0..limit {
    sum = sum + k;
    match k % 3 {
        case 0 { sum = sum + 1; }
        case 1 { sum = sum * 1; }
        case _ { sum = sum - 1; }
    }
}
print(sum);
